apiVersion: v1
kind: Service
metadata:
  name: {{ template "northwindwebui.fullname" . }}
  labels:
    app: {{ template "northwindwebui.name" . }}
    chart: {{ template "northwindwebui.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "northwindwebui.name" . }}
    release: {{ .Release.Name }}
