apiVersion: v1
kind: Service
metadata:
  name: {{ template "payrollapi.fullname" . }}
  labels:
    app: {{ template "payrollapi.name" . }}
    chart: {{ template "payrollapi.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "payrollapi.name" . }}
    release: {{ .Release.Name }}
